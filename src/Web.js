import React from 'react';
import Graph from "./Graph"

const Web = ({type}) => {
    return(
        <div style={{display:"flex"}}>
            <Graph></Graph>
        </div>
    )
}

export default Web